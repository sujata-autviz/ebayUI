export interface QuestionPayload {
    question: string;
    answer: string;
}

export interface Question {
    answer: string;
    created_at: string;
    id: 1;
    question: string;
    updated_at: string;
}
