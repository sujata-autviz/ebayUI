import { EmojiValidationDirective } from './emoji-validation.directive';

describe('EmojiValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmojiValidationDirective();
    expect(directive).toBeTruthy();
  });
});
